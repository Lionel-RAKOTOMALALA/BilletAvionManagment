<Window x:Class="AvionManagment.UpdateVolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mise à jour de vol" Height="700" Width="750"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Style pour les TextBox modernes avec placeholder -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="12,0,12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SelectionBrush" Value="#A5B4FC"/>
            <Setter Property="CaretBrush" Value="#6C63FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2" Color="#000000"/>
                            </Border.Effect>
                            <Grid>
                                <!-- Utilisation d'un TextBlock pour afficher le texte au lieu de ScrollViewer -->
                                <TextBox x:Name="PART_EditableTextBox"
                                         Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="{TemplateBinding Padding}"
                                         Foreground="{TemplateBinding Foreground}"
                                         FontFamily="{TemplateBinding FontFamily}"
                                         FontSize="{TemplateBinding FontSize}"
                                         VerticalContentAlignment="Center"
                                         SelectionBrush="{TemplateBinding SelectionBrush}"
                                         CaretBrush="{TemplateBinding CaretBrush}"
                                         HorizontalScrollBarVisibility="Hidden"
                                         VerticalScrollBarVisibility="Hidden"/>
                                <TextBlock x:Name="placeholderText" 
                                           Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                           Foreground="#AAAAAA"
                                           Padding="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           FontSize="{TemplateBinding FontSize}"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="placeholderText" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8" Color="#406C63FF" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour les ComboBox modernes -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="10">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2" Color="#000000"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="toggleButton" 
                                                  BorderThickness="0"
                                                  Background="Transparent" 
                                                  Focusable="False"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid Background="Transparent">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="30" />
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.ColumnSpan="2" Background="Transparent"/>
                                                    <Path x:Name="arrow" Grid.Column="1" Fill="#555555" 
                                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          Data="M7,10L12,15L17,10H7Z" Width="12" Height="12"/>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Fill" TargetName="arrow" Value="#333333"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <!-- Utilisation d'un TextBlock pour afficher le texte sélectionné -->
                                    <TextBlock x:Name="contentPresenter" 
                                              Text="{TemplateBinding Text}"
                                              IsHitTestVisible="False"
                                              Margin="{TemplateBinding Padding}"
                                              Foreground="{TemplateBinding Foreground}"
                                              FontFamily="{TemplateBinding FontFamily}"
                                              FontSize="{TemplateBinding FontSize}"
                                              HorizontalAlignment="Left" 
                                              VerticalAlignment="Center"/>
                                    <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" 
                                               Foreground="#AAAAAA" IsHitTestVisible="False"
                                               Margin="{TemplateBinding Padding}"
                                               HorizontalAlignment="Left" 
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                    <Popup x:Name="PART_Popup" AllowsTransparency="True" 
                                           IsOpen="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                           Placement="Bottom" 
                                           PopupAnimation="Slide"
                                           Focusable="False">
                                        <Border x:Name="dropDownBorder" Background="White" 
                                                CornerRadius="10" BorderThickness="1" BorderBrush="#20000000"
                                                MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=border}">
                                            <Border.Effect>
                                                <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="#000000"/>
                                            </Border.Effect>
                                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8" Color="#406C63FF" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Visibility" TargetName="placeholder" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter Property="Visibility" TargetName="placeholder" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Visibility" TargetName="placeholder" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour les DatePicker modernes -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="10">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2" Color="#000000"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                                      BorderThickness="0" 
                                                      Padding="{TemplateBinding Padding}"
                                                      Foreground="#333333"
                                                      Background="Transparent"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Center"
                                                      HorizontalContentAlignment="Left"
                                                      VerticalContentAlignment="Center"
                                                      FontSize="{TemplateBinding FontSize}">
                                        <DatePickerTextBox.Style>
                                            <Style TargetType="DatePickerTextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="DatePickerTextBox">
                                                            <Grid>
                                                                <TextBox x:Name="PART_TextBox" 
                                                                         Text="{Binding Path=SelectedDate, RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}, StringFormat='dd/MM/yyyy'}"
                                                                         Foreground="#333333"
                                                                         Background="Transparent"
                                                                         BorderThickness="0"
                                                                         Padding="{TemplateBinding Padding}"
                                                                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                         VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                         SelectionBrush="#A5B4FC"
                                                                         CaretBrush="#6C63FF"/>
                                                            </Grid>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </DatePickerTextBox.Style>
                                    </DatePickerTextBox>
                                    <Button x:Name="PART_Button" Grid.Column="1" Margin="0,0,10,0"
                                            Focusable="False" HorizontalAlignment="Right"
                                            VerticalAlignment="Center" Background="Transparent" BorderThickness="0">
                                        <Button.Content>
                                            <Path Fill="#555555" Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1" 
                                                  Width="16" Height="16" Stretch="Uniform"/>
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" StaysOpen="False" 
                                   AllowsTransparency="True" 
                                   PlacementTarget="{Binding ElementName=PART_Button}">
                                <Border Background="White" CornerRadius="10" BorderThickness="1" BorderBrush="#20000000">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Calendar x:Name="PART_Calendar" BorderThickness="0" Background="Transparent"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6C63FF"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8" Color="#406C63FF" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour les boutons -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.3" Color="#000000"/>
                            </Border.Effect>
                            <ContentPresenter x:Name="contentPresenter" 
                                             HorizontalAlignment="Center" 
                                             VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" TargetName="border" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" TargetName="border" Value="0.8"/>
                                <Setter Property="Effect" TargetName="border">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="#000000"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" TargetName="border" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour le bouton de fermeture -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour les labels -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <!-- Conteneur principal avec transformation -->
    <Grid x:Name="MainContainer">
        <Grid.RenderTransform>
            <ScaleTransform x:Name="WindowScaleTransform" ScaleX="1" ScaleY="1" />
        </Grid.RenderTransform>

        <!-- Fond principal avec effet de flou -->
        <Border CornerRadius="20" Margin="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#70000000"/>
            </Border.Effect>

            <Grid>
                <!-- Fond avec dégradé moderne -->
                <Border CornerRadius="20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366F1" Offset="0"/>
                            <GradientStop Color="#4F46E5" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <!-- Éléments décoratifs -->
                <Canvas>
                    <!-- Cercles décoratifs -->
                    <Ellipse Width="300" Height="300" Canvas.Left="-100" Canvas.Top="-100" Opacity="0.1">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Ellipse Width="200" Height="200" Canvas.Right="-50" Canvas.Bottom="-50" Opacity="0.1">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#FFFFFF" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- Formes géométriques -->
                    <Path Data="M0,0 L50,0 L25,40 Z" Fill="#20FFFFFF" Canvas.Right="100" Canvas.Top="100"/>
                    <Path Data="M0,0 L40,0 L40,40 L0,40 Z" Fill="#15FFFFFF" Canvas.Left="150" Canvas.Bottom="120" RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <RotateTransform Angle="30"/>
                        </Path.RenderTransform>
                    </Path>
                </Canvas>

                <!-- Contenu -->
                <Grid Margin="40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- En-tête avec icône d'avion -->
                    <Grid Grid.Row="0" Margin="0,0,0,30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="60" Height="60" CornerRadius="30" Margin="0,0,20,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#A5B4FC" Offset="0"/>
                                        <GradientStop Color="#818CF8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.3" Color="#000000"/>
                                </Border.Effect>
                                <Path Data="M21,16V14L13,9V3.5A1.5,1.5 0 0,0 11.5,2A1.5,1.5 0 0,0 10,3.5V9L2,14V16L10,13.5V19L8,20.5V22L11.5,21L15,22V20.5L13,19V13.5L21,16Z" 
                                      Fill="White" Width="30" Height="30" Stretch="Uniform"/>
                            </Border>
                            <StackPanel>
                                <TextBlock Text="Mise à jour de vol" FontSize="32" FontWeight="Bold" Foreground="White" 
                                       HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Color="#50000000" />
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Modifiez les informations du vol" Foreground="#E0E7FF" 
                                       FontSize="14" Margin="2,5,0,0"/>
                            </StackPanel>
                        </StackPanel>
                        <Button x:Name="CloseButton" Content="×" Style="{StaticResource CloseButtonStyle}" 
                                HorizontalAlignment="Right" VerticalAlignment="Top" 
                                Click="CloseButton_Click"/>
                    </Grid>

                    <!-- Formulaire - Utilisation d'un Grid pour un meilleur contrôle -->
                    <Grid Grid.Row="1" Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- ID du vol (lecture seule) -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,25">
                            <TextBlock Text="ID du vol" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <TextBlock Text="ID" FontSize="18" FontWeight="Bold" Foreground="White" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBox x:Name="txtID" Grid.Column="1" Style="{StaticResource ModernTextBox}" 
                                         Tag="ID du vol" IsReadOnly="True" Background="#F0F0F0"/>
                            </Grid>
                        </StackPanel>

                        <!-- Date et heure de départ -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,25">
                            <TextBlock Text="Date et heure de départ" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Path Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1" 
                                          Fill="White" Width="22" Height="22" Stretch="Uniform"/>
                                </Border>
                                <DatePicker x:Name="dpDateTimeDepart" Grid.Column="1" Style="{StaticResource ModernDatePicker}" 
                                            Tag="Sélectionnez une date"/>
                            </Grid>
                        </StackPanel>

                        <!-- Date et heure d'arrivée -->
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,0,0,25">
                            <TextBlock Text="Date et heure d'arrivée" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Path Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1" 
                                          Fill="White" Width="22" Height="22" Stretch="Uniform"/>
                                </Border>
                                <DatePicker x:Name="dpDateTimeArrive" Grid.Column="1" Style="{StaticResource ModernDatePicker}" 
                                            Tag="Sélectionnez une date"/>
                            </Grid>
                        </StackPanel>

                        <!-- Aéroport de départ -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,25">
                            <TextBlock Text="Aéroport de départ" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Path Data="M2.5,19H21.5V21H2.5V19M22.07,9.64C21.86,8.84 21.03,8.36 20.23,8.58L14.92,10L8,3.57L6.09,4.08L11.03,11.35L5.26,12.91L3.29,11.47L2.26,11.9L4.69,16.57L5.69,16.18L13.22,13.68L18.3,18.13L20.25,17.62L17.35,12.79L21.8,11.46L22.07,9.64Z" 
                                          Fill="White" Width="22" Height="22" Stretch="Uniform"/>
                                </Border>
                                <ComboBox x:Name="cmbAeroportDepart" Grid.Column="1" Style="{StaticResource ModernComboBox}" 
                                          Tag="Aéroport de départ"/>
                            </Grid>
                        </StackPanel>

                        <!-- Aéroport d'arrivée -->
                        <StackPanel Grid.Row="2" Grid.Column="1" Margin="15,0,0,25">
                            <TextBlock Text="Aéroport d'arrivée" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Path Data="M2.5,19H21.5V21H2.5V19M9.68,13.27L14.03,14.43L19.34,15.85C20.14,16.06 20.96,15.59 21.18,14.79C21.39,14 20.92,13.17 20.12,12.95L14.81,11.53L12.05,2.5L10.12,2V10.28L5.15,8.95L4.22,6.63L2.77,6.24V11.41L4.37,11.84L9.68,13.27Z" 
                                          Fill="White" Width="22" Height="22" Stretch="Uniform"/>
                                </Border>
                                <ComboBox x:Name="cmbAeroportArrive" Grid.Column="1" Style="{StaticResource ModernComboBox}" 
                                          Tag="Aéroport d'arrivée"/>
                            </Grid>
                        </StackPanel>

                        <!-- Avion -->
                        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,15,25">
                            <TextBlock Text="Avion" Style="{StaticResource LabelStyle}" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" 
                                        Margin="0,0,12,0" VerticalAlignment="Center">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A5B4FC" Offset="0"/>
                                            <GradientStop Color="#818CF8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2" Color="#000000"/>
                                    </Border.Effect>
                                    <Path Data="M21,16V14L13,9V3.5A1.5,1.5 0 0,0 11.5,2A1.5,1.5 0 0,0 10,3.5V9L2,14V16L10,13.5V19L8,20.5V22L11.5,21L15,22V20.5L13,19V13.5L21,16Z" 
                                          Fill="White" Width="22" Height="22" Stretch="Uniform"/>
                                </Border>
                                <ComboBox x:Name="cmbAvion" Grid.Column="1" Style="{StaticResource ModernComboBox}" 
                                          Tag="Sélectionnez un avion"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Boutons -->
                    <Grid Grid.Row="2" Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="btnCancel" Grid.Column="1" Content="Annuler" Margin="0,0,20,0" 
                                Style="{StaticResource ModernButton}" Click="BtnCancel_Click" Width="180" Height="55">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#F87171" Offset="0"/>
                                    <GradientStop Color="#EF4444" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>

                        <Button x:Name="btnSave" Grid.Column="2" Content="Mettre à jour" 
                                Style="{StaticResource ModernButton}" Click="BtnSave_Click" Width="180" Height="55">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#818CF8" Offset="0"/>
                                    <GradientStop Color="#6366F1" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
